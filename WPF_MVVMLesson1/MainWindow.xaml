<Window x:Class="WPF_MVVMLesson1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MVVMLesson1"
        xmlns:vm="clr-namespace:WPF_MVVMLesson1.ViewModels"
        mc:Ignorable="d"
        Title="MVVM" Height="200" Width="250" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <StackPanel Margin="5" Background="AliceBlue">
            <Button Margin="5" Width="50" Height="20" HorizontalAlignment="Left" Content="Save" Command="{Binding SaveCommand}" />
            <TextBox Height="20" Margin="5" Text="{Binding Input1}" />
            <TextBox Height="20" Margin="5" Text="{Binding Input2}" />
            <TextBox Height="20" Margin="5" Text="{Binding Result}" />
            <Button Margin="5" Width="50" Height="20" HorizontalAlignment="Right" Content="Result" Command="{Binding AddCommand}" />
        </StackPanel>
    </Grid>
</Window>